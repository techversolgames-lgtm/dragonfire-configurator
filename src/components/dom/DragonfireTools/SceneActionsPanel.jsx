/**
 * Screen panel at middle left with actions for the selected scene item.
 * Remove-from-scene is handled in DragonfireToolsSidebar (cabinet section for cabinets, room item section for room items).
 */
export default function SceneActionsPanel() {
  return null;
}
